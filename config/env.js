import dotenv from 'dotenv';

// This file has one single purpose: load the environment variables from the .env file.
// It should be imported at the very top of the main server file.
dotenv.config();
